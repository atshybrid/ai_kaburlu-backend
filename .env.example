# Domain Configuration Examples
# =========================

###############################################
# Simple dev/prod switching via ENV_TYPE (optional)
# Use ENV_TYPE=dev or ENV_TYPE=prod. If omitted, NODE_ENV is used.
ENV_TYPE=dev

# Database URLs (preferred when using ENV_TYPE)
DEV_DATABASE_URL=postgresql://username:password@localhost:5432/dev_db
PROD_DATABASE_URL=postgresql://username:password@db-host:5432/prod_db
# Fallback (if DEV_/PROD_ not provided)
DATABASE_URL="postgresql://username:password@localhost:5432/database_name"

# Base URLs (used by the backend for building links)
DEV_BASE_URL=http://localhost:3000
PROD_BASE_URL=https://api.yourdomain.com
BASE_URL=

# Admin bootstrap guard (token-based, no JWT): used by POST /api/v1/admin/bootstrap-accounts
# Set a strong random value, use once to create initial admins, then rotate/remove
ADMIN_BOOTSTRAP_TOKEN=change-this-strong-token

###############################################
# Environment setting (still supported)
NODE_ENV=development
# NODE_ENV=staging
# NODE_ENV=production

###############################################
# Prisma migrate on startup (optional)
# Set to 1 to enable; choose strategy: deploy (with migrations) or push (dev sync)
PRISMA_MIGRATE_ON_START=0
PRISMA_MIGRATE_MODE=deploy
PRISMA_MIGRATE_TIMEOUT_MS=60000
# If set to 1, server will exit on migrate failure
PRISMA_MIGRATE_FAIL_FATAL=0

###############################################
# Seed on startup (optional)
# Enable to run a seed once on startup
SEED_ON_START=0
# Runner: 'script' (node compiled dist script) or 'npm' (run npm script)
SEED_RUNNER=script
# When runner=script: path under scripts/ (compiled to dist/scripts)
# Example: scripts/seed_hrci_samples.ts
SEED_SCRIPT=
# When runner=npm: the npm script name to run (e.g., seed or seed:hrci:samples)
SEED_NPM_SCRIPT=
SEED_TIMEOUT_MS=120000
SEED_FAIL_FATAL=0

# Development Domain (used when NODE_ENV=development)
DEV_DOMAIN=http://localhost:3000

# Staging Domain (used when NODE_ENV=staging)
STAGING_DOMAIN=https://staging.hrcitodaynews.in

# Production Domain (used when NODE_ENV=production)
PROD_DOMAIN=https://app.hrcitodaynews.in

# Override Domains (takes priority over environment-specific domains)
# CANONICAL_DOMAIN=https://custom.domain.com
# EXTRA_DOMAIN=https://extra.domain.com

# URL Examples Generated:
# Development: http://localhost:3000/en/short/breaking-news-slug
# Staging: https://staging.hrcitodaynews.in/en/short/breaking-news-slug  
# Production: https://app.hrcitodaynews.in/en/short/breaking-news-slug
# With CANONICAL_DOMAIN: https://custom.domain.com/en/short/breaking-news-slug
# With EXTRA_DOMAIN: https://extra.domain.com/en/short/breaking-news-slug

# Firebase Configuration (existing)
FIREBASE_PROJECT_ID=khabarx-f0365
FIREBASE_CLIENT_EMAIL=your-service-account@khabarx-f0365.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY\n-----END PRIVATE KEY-----\n"

# Google OAuth Configuration (existing)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Database Configuration (existing) – see DEV_/PROD_ above
# DATABASE_URL="postgresql://username:password@localhost:5432/database_name"

# JWT Secrets (existing)
JWT_SECRET=your-jwt-secret
JWT_REFRESH_SECRET=your-jwt-refresh-secret

# Razorpay (dev/prod)
DEV_RAZORPAY_KEY_ID=
DEV_RAZORPAY_KEY_SECRET=
DEV_RAZORPAY_WEBHOOK_SECRET=
DEV_PAYMENT_CALLBACK_BASE_URL=http://localhost:3000

PROD_RAZORPAY_KEY_ID=
PROD_RAZORPAY_KEY_SECRET=
PROD_RAZORPAY_WEBHOOK_SECRET=
PROD_PAYMENT_CALLBACK_BASE_URL=https://app.yourdomain.com